name: Auto Version Bump

on:
  push:
    branches: [ main ]

permissions:
  contents: write

jobs:
  bump-version:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Generate content hash
        id: hash
        run: |
          HASH=$(cat *.js style.css index.html 2>/dev/null | sha256sum | cut -c1-12)
          BUILD="$(date +%Y%m%d)-${HASH}"
          echo "build=${BUILD}" >> $GITHUB_OUTPUT
          echo "APP_BUILD = ${BUILD}"

      - name: Inject APP_BUILD into index.html
        run: |
          BUILD="${{ steps.hash.outputs.build }}"
          sed -i "/\/\//! s|window\.APP_BUILD = '[^']*';|window.APP_BUILD = '${BUILD}';|" index.html
          echo "index.html updated: APP_BUILD = ${BUILD}"

      - name: Verify APP_BUILD injected
        run: |
          BUILD="${{ steps.hash.outputs.build }}"
          if ! grep -q "window\.APP_BUILD = '${BUILD}';" index.html; then
            echo "APP_BUILD gagal di-inject."
            exit 1
          fi
          echo "Verified: window.APP_BUILD = '${BUILD}'"

      - name: Bump JS_CACHE version in sw.js
        run: |
          BUILD="${{ steps.hash.outputs.build }}"
          # Ganti nilai JS_CACHE dengan versi baru berdasarkan build hash
          # Format: zenot-js-<12 char hash> → SW selalu fetch ulang semua JS setiap deploy
          NEW_CACHE="zenot-js-${BUILD}"
          sed -i "s|var JS_CACHE = '[^']*';|var JS_CACHE = '${NEW_CACHE}';|" sw.js
          echo "sw.js updated: JS_CACHE = ${NEW_CACHE}"

      - name: Commit updated files
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add index.html sw.js
          if git diff --staged --quiet; then
            echo "Tidak ada perubahan, skip commit."
          else
            git commit -m "chore: auto bump APP_BUILD + JS_CACHE -> ${{ steps.hash.outputs.build }} [skip ci]"
            git push
          fi
